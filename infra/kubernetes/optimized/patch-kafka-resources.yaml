apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: vacciguard
spec:
  template:
    spec:
      containers:
        - name: kafka
          resources:
            requests:
              cpu: "700m"
              memory: "1536Mi"
            limits:
              cpu: "1500m"
              memory: "3Gi"
