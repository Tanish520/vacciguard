apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: vacciguard
spec:
  template:
    spec:
      containers:
        - name: kafka
          resources:
            requests:
              cpu: "1000m"
              memory: "2Gi"
            limits:
              cpu: "2000m"
              memory: "4Gi"
