apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: vacciguard
spec:
  template:
    spec:
      containers:
        - name: kafka
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
